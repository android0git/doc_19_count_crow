<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        
        <item name="android:windowTranslucentNavigation">true</item>    // Делаем панель навигации прозрачной (API 19+)
        // <item name="android:navigationBarColor">@color/colorPrimary</item>

        <item name="android:windowTranslucentStatus">true</item>    // Делаем строку состояния прозрачной (API 19+).

        // Переопределяем внешний вид стандартного значка меню в виде трёх точек по вертикальной линии.
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
    </style>



    <style name="OverFlow" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_myoverflow</item>
    </style>

</resources>
